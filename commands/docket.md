---
description: Show the decision ledger for this project, or record a decision
---

Run `docket list` and show what it prints, grouped by state.

If the user passed arguments, treat them as a decision to record. Work out the
question, the answer, and the state from what they said, confirm the phrasing in
one line, then run `docket add`.

Arguments: $ARGUMENTS
